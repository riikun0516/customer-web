<?xml version="1.0" encoding="UTF-8"?>
<!-- IIS環境向け: このディレクトリへの直接アクセスを禁止する（.htaccessはIISでは効かないため） -->
<configuration>
  <system.webServer>
    <security>
      <authorization>
        <clear />
        <deny users="*" />
      </authorization>
    </security>
  </system.webServer>
</configuration>
